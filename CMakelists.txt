cmake_minimum_required(VERSION 3.15)

set(CMAKE_C_COMPILER "D:/msys64/ucrt64/bin/gcc.exe" )
set(CMAKE_CXX_COMPILER "D:/msys64/ucrt64/bin/g++.exe" )
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -static")
set (CMAKE_CXX_STANDARD 20)

project(Showcase VERSION 1.0
                  DESCRIPTION "Showcase Project"
                  LANGUAGES C CXX)

#set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${CMAKE_CURRENT_SOURCE_DIR}/cmake)

make_directory( ${CMAKE_SOURCE_DIR}/build )

find_package(Vulkan REQUIRED)
include_directories( ${VULKAN_INCLUDE_DIRS} )

#find_package(glfw3 REQUIRED)

include_directories(include)

file(GLOB SOURCES "src/*.cpp")

add_executable(showcase ${SOURCES})

include(FetchContent)

set(FETCHCONTENT_BASE_DIR ${PROJECT_SOURCE_DIR}/libs CACHE PATH "Missing description." FORCE)

FetchContent_Declare(
	glm
	GIT_REPOSITORY	https://github.com/g-truc/glm.git
	GIT_TAG 	0af55ccecd98d4e5a8d1fad7de25ba429d60e863 #refs/tags/1.0.1
)

FetchContent_Declare(
	assimp
	GIT_REPOSITORY https://github.com/assimp/assimp.git
	GIT_TAG master #v6.0.5
)

FetchContent_Declare(
	ktx
	GIT_REPOSITORY https://github.com/KhronosGroup/KTX-Software.git
	GIT_TAG 16059fa93d834242a0aaed9bb54952d76b143fac #v5.0.0-rc2
)

FetchContent_Declare(
	slang
	URL https://github.com/shader-slang/slang/releases/download/v2026.17/slang-2026.17-windows-x86_64.zip
	#URL_HASH MD5=5bcb1c031b7873444f745b757a35e25567bbe043 #v2026.17
)

set(KTX_FEATURE_STATIC_LIBRARY ON CACHE BOOL "Build KTX as a static library" FORCE)
set(BUILD_SHARED_LIBS OFF CACHE BOOL "" FORCE)
set(ASSIMP_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(ASSIMP_INJECT_DEBUG_POSTFIX OFF CACHE BOOL "" FORCE)
set(ASSIMP_INSTALL OFF CACHE BOOL "" FORCE)
set(SLANG_LIB_TYPE ON CACHE BOOL "STATIC" FORCE)

FetchContent_MakeAvailable(glm)
FetchContent_MakeAvailable(assimp)
FetchContent_MakeAvailable(ktx)
FetchContent_MakeAvailable(slang)

target_include_directories(showcase PRIVATE libs/assimp-src/include)
target_include_directories(showcase PRIVATE libs/slang-src/include)
target_link_directories(showcase PRIVATE libs/slang-src/lib)

target_include_directories(showcase PRIVATE include/GLFW)
target_link_directories(showcase PRIVATE lib/GLFW)

target_link_libraries(showcase PRIVATE glfw3 Vulkan::Vulkan assimp glm ktx slang -static-libgcc -static-libstdc++)
